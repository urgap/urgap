{
    "info": "Urgap will internally reformat this config into a simple dictionary (k, v[v'value']) available under urgap.config",
    "description": "urgap default config",
    "umeta": {
        "value": "sqlite3",
        "description": "Storage interface for meta data. Might require 3rd party installations",
        "options": ["sqlite3", "postgresql", "gcpsql"]
    },
    "logging_level": {
        "value": "DEBUG",
        "description": "Verbosity of urgap, can be set to any python logger level"
    },
    "update_resources": {
        "value": false,
        "description": "Define whether resources from the repo will be force copied into $URGAP_HOME on startup. This is useful if resources are developed in the urgap repo, yet code execution will use $URGAP_HOME/resource code."
    },
    "update_uparma": {
        "value": false,
        "description": "Define whether uparma json download should be forced (part of u2 dev work). Not required for release"
    },
    "hash_algorithm": {
        "value": "md5",
        "description": "Hash algorithm used in Urgap in order to create unique identifiers"
    },
    "max_parallel_cores": {
        "value": null,
        "description": "Number of CPU cores to use for parallel processing."
    },
    "umeta-postgresql-url": {
        "value": "postgresql://localhost:5432",
        "description": "url for postgresql instance, used if umeta is set to postgresql. Login and password are extracted via ENV or alternative secret store defined in credentials_lookup.json"
    },
    "umeta-sqlite3-url": {
        "value": null,
        "description": "url for sqlite3 db file, used if umeta is set to sqlite. Should start with file:///"
    },
    "umeta-gcpsql-url": {
        "value": null
    },
    "scratch_disk": {
        "value": null,
        "description": "Temporary urgap processing disk"
    },
    "mode": {
        "value": "dev",
        "description": "Mode in which urgap is running. Can be set to prod for stricter rules.",
        "options": ["dev", "prod"]
    },
    "clean_scratch_at_exit": {
        "value": true,
        "description": "Urgap uses scratch <scratch_disk>/<wid> as temporary spaces. This flag will ensure that temporary directories (<wid>s) will be deleted when interpreter exits."
    },
    "requests_timeout_connect": {
        "value": null,
        "description": "Connect timeout for python requests"
    },
    "requests_timeout_read": {
        "value": null,
        "description": "Read timeout for python requests"
    },
    "terminate_remote_servers_on_exit": {
        "value": false,
        "description": "Send a termination signal to all ports on localhost during urgap exit"
    },
    "opentelemetry_collector_url": {
        "value": null,
        "description": "Open Telementry Collector url, e.g. http://localhost:4317"
    },
    "opentelemetry_exporter_type": {
        "value": null,
        "description": "Open Telementry Exporter Type. Null will disable OTEL.",
        "options": [null, "OTPL", "AZ-Insights", "Console"]
    },
    "service_bus_topic": {
        "value": "urgap_queue",
        "description": "Topic for pulling work from Service Bus worker."
    },
    "service_bus_completion_topic": {
        "value": "urgap_completed",
        "description": "Optional topic to publish completion events to."
    },
    "service_bus_exit_after_first_message": {
        "value": true,
        "description": "If true, worker exits successfully after processing one message."
    },
    "service_bus_max_autorenewal_minutes": {
        "value": 0,
        "description": "Maximum time after which to stop auto renewing lock on message. 0 disables this."
    }
}
