trigger:
  branches:
    include:
      - main
pr:
  branches:
    include:
      - main
      - feature/*
      - fix/*
resources:
  repositories:
    - repository: templates
      name: gsk-tech/dso_azure_pipeline_templates
pool:
  vmImage: ubuntu-latest
stages: